{% extends 'form/form.html.twig' %}


{% block body %}

    {{ parent() }}
    <h1>Modifier categorie</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
    <div class="row">
        <div class="col s3">
            <input type="submit" value="Modifier" class="btn waves-effect waves-light green" />
        </div>
    </div>
    {{ form_end(edit_form) }}

    <div class="row">
        <div class="col s3">
            <a href="{{ path('administration_category_index') }}"> <input type="button" class="btn waves-effect waves-light" value="retourner à la liste" /></a>
        </div>
    </div>

    {{ form_start(delete_form) }}
    <div class="row">
        <div class="col s3">
            <input type="submit" value="Supprimer" class="waves-effect waves-light btn red">
        </div>
    </div>
    {{ form_end(delete_form) }}


{% endblock %}
